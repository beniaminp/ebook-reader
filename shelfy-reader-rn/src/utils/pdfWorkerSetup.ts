/**
 * PDF.js worker setup stub for React Native.
 *
 * In the Ionic web app this configures the PDF.js GlobalWorkerOptions.workerSrc
 * to a CDN URL. In React Native, PDF rendering is handled natively via
 * react-native-pdf or a WebView, so no worker setup is needed.
 *
 * This stub exists so that any code ported from the web app that imports
 * this module won't break at compile time.
 */

// No-op: PDF.js worker is not used in React Native.
